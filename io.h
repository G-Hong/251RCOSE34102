#ifndef IO_H
#define IO_H

#include "process.h"

//TODO
//I/O 발생 요청 처리 모듈
//io 요청이 발생하면 프로세스를 waiting queue로 이동시키고 io 시간이 끝나면 다시 ready queue로 복귀시키는 역할

//void handle_io_request(Process *p, int current_time);
//void update_io_queues(int current_time);
//int is_to_done(Process *p, int current_time);

#endif